Progress:
	finished git.py class (needs bugtested extensively)

functionality()
#1. logger
#	A. TODO: add origin file/module/function 'append' to string for origination of more than just exceptions
#		(know where a log line comes from)
2. backup
	A. zip
		a. ...
		b. ...
	B. Manage Backups:
		0. Create backup manager window
		1. Add listbox with relevant backups for pm.project_name
		2. Sort by timestamp > filename (newest first)
		3. Add Backup button in main.py
		4. Add Restore button in main.py
		5. Add Delete Backup button in main.py
		6. Add Set Backup Path button in main.py:
			a. ui.file_browser(browse_type='folder')
3. vars
	A. GITDIR
	B. INSTALL_DIR
	C. BACKUP_DIR
	D. TARFILE
4. psg_creator():
	A. Integrate ui editor and functionality modules into project manager
5. directory migration

-----------------------------

gui for configuration of functionality

To be continued...

modules:
#git.py
#tar.py
#zip.py(???)
#log.py
filesystem.py:
	#1. copy
	#2. mv
	#3. rm
	#4. ls
	#5. mkdir
	#6. touch
	#7. write
	#8. read
	#9. find_all
maintenance
	1. junk removal
	2. temp removal
	3. backup management

